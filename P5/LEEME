Contenido de cada archivo:

AGG.py: fichero donde está programado el AGG

AM.py: fichero en el que están implementados los tres algoritmos meméticos.

BLCUDA.py: fichero donde está implementada la búsqueda local normal.

BLCUDA1iter.py: fichero donde está implementada la búsqueda local de baja intensidad usada por los algoritmos meméticos.

kernel.cu: fichero donde está el código CUDA para el clasificador

knnLooGPU.py: interfaz en Python para usar el kNN programado en CUDA (está la clase que usamos para crear clasificadores)

mainCUDA.py: archivo con el que ejecutamos los experimentos para cada algoritmo

En el directorio make_partitions32b tenemos los datos necesarios para construir las particiones:

Tenemos los ficheros arff proporcionados para la práctica

loader.py: fichero que carga dichos arffs y los formatea almacenando los datos formateados en
los ficheros
						data_arrythmia.npy
						data_libras.npy
						data_wdbc.npy

partition_maker.py: archivo donde se implementa el código para crear las particiones

En el directorio partitions32b se almacenan los archivos .npy que contienen cada una de las
particiones creadas antes.
